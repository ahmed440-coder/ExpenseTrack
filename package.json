{
  "name": "expense-tracker-root",
  "version": "1.0.0",
  "description": "Root package.json to deploy MERN app",
  "scripts": {
    "build": "npm install && cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
